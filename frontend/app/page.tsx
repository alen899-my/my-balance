"use client";

/**
 * Dashboard Root Page
 * Content is managed via the Layout (Sidebar & Header).
 * This page is kept empty as per requirement.
 */
export default function DashboardPage() {
  return (
    <div className="w-full h-full">
      {/* Page content removed. 
          The Sidebar and Header are handled by app/(dashboard)/layout.tsx 
      */}
    </div>
  );
}